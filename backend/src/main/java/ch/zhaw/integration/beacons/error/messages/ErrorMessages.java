package ch.zhaw.integration.beacons.error.messages;

public class ErrorMessages {
	
	//Error message used if a user is already signed up with email
	public static String EMAIL_ALREADY_USED = "Email is already used.";

	//Error message used if a login failed due to wrong credentials
	public static String EMAIL_OR_PW_WRONG = "Wrong credentials.";
 
}
